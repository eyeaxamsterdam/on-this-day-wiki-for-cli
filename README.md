This is a simple CLI program written with NodeJS to bring wikipedia to the
terminal. 

After cloning, cd into the app folder and run "npm install $$ npm link"

Now you can run the program with "otdih"
